// Variáveis

#include <stdio.h>

int main(){
    // Declarando variáveis
    int idade;
    
    // Entrada de dados
    printf("Digite a sua idade: ");
    scanf("%d", &idade);

    // Saída de dados
    printf("A idade digitada foi: %d\n", idade);

    return 0;
}