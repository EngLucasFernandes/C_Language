#include <stdio.h>

int main(){
    // Declaração de variáveis
    int number;

    // Entrada de dados
    printf("Enter a integer number: ");
    scanf("%d", &number);

    // Saída de dados
    printf("The integer number is: %d", number);

    return 0;
}