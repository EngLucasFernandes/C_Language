#include <stdio.h>

int main(){

    // Declaração de variáveis
    float number;

    // Entrada de dados
    printf("Enter a number: ");
    scanf("%f", &number);

    // Saída de dados
    printf("The number is: %.2f", number);

    return 0;
}